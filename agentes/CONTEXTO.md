# Contexto do Projeto — Equalizador

## 1. Visão geral do projeto

**Propósito:** app desktop de uso pessoal (Bruno) para modificar a própria voz em tempo real durante jogos online e tocar efeitos sonoros ("soundboard") audíveis pelos outros participantes, sem sair do jogo.

**Domínio:** processamento de áudio em tempo real / voice changer / soundboard para gaming.

**Plataforma-alvo:** Windows apenas (não há necessidade de macOS/Linux).

**Jogo-alvo principal:** Call of Duty (anti-cheat Ricochet, kernel-level).

**Stack recomendada:** C++ com JUCE (áudio + UI no mesmo framework), WASAPI modo compartilhado como API de áudio padrão.

**Status atual:** segunda rodada de implementação concluída e validada em Windows real (não só no Mac de dev) — build via CMake+MSVC, app rodando de ponta a ponta com VB-CABLE instalado, roteando a voz processada para o mic virtual sem depender do dispositivo de saída padrão do Windows (bug real encontrado e corrigido nesta rodada — ver seção 4). Voice changer com 6 vozes (Normal, Robô, Monstro, Alien/ET, Esquilo, Demônio) e tuning de timbre lembrado por efeito. Soundboard com 12 slots (cobertura completa de F1–F12), cada slot com arquivo e hotkey (aceitando um modificador — Ctrl/Alt/Shift) escolhidos pelo usuário e persistidos entre execuções; bypass também configurável pela mesma UI de captura de tecla. 1281 testes unitários passando. UI funcional, ainda não o design visual completo do Designer. Revisor aprovou todas as rodadas (uma delas só após um ciclo de retrabalho por race condition crítica — ver log). Detalhes no log de atualizações.

## 2. Arquitetura

Fluxo de dados: microfone físico → **Capture Layer** (WASAPI) → **DSP/Effects Layer** (voice changer: pitch shift/robô/monstro primeiro; EQ multibanda depois, como mais um nó da cadeia) → **Mixer Layer** (soma voz processada + saída do **Soundboard Layer**, com ganhos independentes) → **Output/Routing Layer** (escreve no endpoint WASAPI de render do driver de áudio virtual) → dispositivo virtual "microfone" selecionável no jogo/Discord.

Camadas transversais:
- **Hotkey/Input Layer** — captura global de teclado (funciona com o jogo em foco), dispara toggle de bypass e reprodução de sons do soundboard via fila lock-free.
- **Bypass/State Layer** — single source of truth do estado ligado/desligado do efeito de voz, lido tanto pelo DSP quanto pela UI (indicador visual).
- **Persistence Layer** — config local em JSON (dispositivos selecionados, efeito/preset ativo, teclas mapeadas, pasta do soundboard, volumes).
- **UI Layer** — seletores de dispositivo, indicador de bypass, botões do soundboard com tecla vinculada. Sem overlay in-game no MVP.
- **Installer/Bootstrap Layer** — embute e instala silenciosamente o driver de áudio virtual (VB-CABLE) durante o setup do app.

**Princípio estrutural chave:** DSP e Mixer não conhecem Windows/WASAPI/drivers — só recebem/entregam buffers PCM. É a fronteira de desacoplamento que permite trocar a estratégia de driver virtual (ou até a API de captura) sem tocar em efeito/mixagem. A camada de Saída trata o endpoint alvo como configurável por nome/ID, nunca hard-coded ao VB-CABLE.

A thread de áudio (Captura→DSP→Mixer→Saída) nunca aloca memória nem toma locks; comunicação com UI/hotkeys é via flags atômicas / estruturas lock-free — requisito de arquitetura, não detalhe de implementação.

## 3. Convenções de código

Estabelecidas na primeira rodada de implementação (Dev):
- Namespace `eq` para todo o código do projeto.
- Contrato de thread-safety documentado no header de cada classe que toca a thread de áudio (o que pode/não pode alocar ou travar).
- Convenção `prepare()` fora da thread de áudio (pode alocar, resolver dispositivos, carregar arquivos) / `process()` (ou equivalente de callback) estritamente RT-safe — sem alocação, sem lock, sem I/O.
- Buffers de trabalho dimensionados uma vez em `prepare()` com `setSize(..., avoidReallocating=true)`; no callback, apenas `jmin(numSamples, capacidadeReservada)`, nunca realoca.
- Camadas de DSP/Mixer/Soundboard/State/Persistence não incluem headers de plataforma (`juce_audio_devices`, `juce_gui_*`) — só `Capture`/`Output`/`Hotkeys`/`MainComponent` tocam plataforma. Validado por grep na revisão.
- Flags de warning do JUCE ativas (ex: `-Wsign-conversion`) tratadas como erro a corrigir, não a silenciar.
- Testes unitários via `juce::UnitTest` (não Catch2) — evita dependência extra já que o código sob teste usa tipos JUCE (`AudioBuffer`, `var`, `File`).

## 4. Decisões importantes e histórico

- **ADR-001 — Dispositivo de áudio virtual via VB-CABLE embutido, instalado silenciosamente pelo instalador do Equalizador.** Decisão motivada por exigência do Bruno de não instalar nada manualmente à parte. Driver próprio do zero foi descartado (exigiria certificado de assinatura EV e homologação com a Microsoft — desproporcional a projeto pessoal). Voicemeeter completo também foi descartado por trazer mixer GUI concorrente com o mixer próprio do Equalizador. Ressalva: VB-CABLE é gratuito para uso pessoal; se o escopo mudar para distribuir o app a terceiros, revisitar licenciamento de redistribuição com a VB-Audio.
- **ADR-002 — C++ com JUCE como framework central de áudio e UI.** Cobre captura, DSP, mixagem e UI num único projeto. Alternativas descartadas: Rust+cpal (ecossistema de efeitos de voz ainda imaturo, exigiria FFI para as mesmas libs C/C++ mesmo assim); .NET/NAudio (risco de stutter na thread de áudio por causa do garbage collector).
- **ADR-003 — Mixagem de voz + soundboard acontece dentro do próprio Equalizador**, não delegada a ferramenta externa (ex: Voicemeeter). Toda a lógica de volume/ganho fica sob controle direto do app.
- **Escopo do "modificar voz":** o Bruno quer voice changer completo (pitch shift, robô, monstro) como prioridade, com EQ de bandas vindo depois — não é só um equalizador simples, apesar do nome do projeto.
- **Soundboard:** apenas atalho de teclado global no MVP, sem overlay visual in-game.
- **Anti-cheat (Ricochet/CoD):** risco avaliado como baixo — drivers de áudio virtual são amplamente usados por streamers/jogadores de CoD sem histórico relevante de banimento (diferente de injeção de processo/leitura de memória, que é o alvo real de anti-cheat). Não é motivo para mudar de abordagem, mas vale validar empiricamente quando o app existir.
- **ADR-004 — Pitch-shift via SoundTouch (fechado pelo TL).** Fonte canônica com tags versionadas: `codeberg.org/soundtouch/soundtouch` 2.4.1 — o mirror `github.com/soundtouch/soundtouch` não expõe tags. Rubber Band fica como plano B só se a qualidade do efeito "monstro" não for suficiente após medição, não por preferência a priori. Licença LGPL: build de dev local usa SoundTouch estático (aceitável para uso pessoal), mas o **release Windows precisa linkar dinamicamente** (ou distribuir objetos relinkáveis) para conformidade — vira item obrigatório do checklist de empacotamento.
- **Decisões de UI do Designer (etapa 5, primeira rodada de código):** tema escuro fixo, sem opção de tema claro no MVP (app fica em segundo plano/segundo monitor ao lado do jogo). Soundboard em grid fixo sem scroll (se precisar de mais sons no futuro, usar abas fixas, não lista rolável) — isso restringe como a estrutura de dados dos sons deve ser modelada (lista limitada, não paginação). Vermelho reservado exclusivamente para estado de erro real ("sem sinal de microfone"), nunca para bypass/voz normal. "Ouvir a mim mesmo" é um checkbox simples, não um segundo seletor de dispositivo de saída — mas o TL registrou que isso implica uma stream WASAPI secundária (`SelfMonitorOutput`) com clock próprio, não é "de graça" tecnicamente (ver seção 6).
- **Ambiente de desenvolvimento é macOS, alvo final é Windows.** Decisão do Bruno: implementação usa `juce::AudioDeviceManager` (abstrai CoreAudio/WASAPI) para que Capture/DSP/Mixer/Soundboard/Persistence/UI compilem e rodem de verdade no Mac durante o desenvolvimento; apenas o que é genuinamente Windows-only (hotkeys globais via `RegisterHotKey`, detecção do VB-CABLE) fica isolado atrás de interface (`IHotkeyBackend`) e só será compilado/testado numa máquina Windows real. Atualização: a partir desta segunda rodada, o Bruno também tem uma máquina Windows real disponível e o pipeline passou a compilar/testar/validar diretamente nela (toolchain CMake+MSVC Build Tools instalada) — o Mac continua sendo o ambiente de dev do dia a dia, mas features que tocam device/hotkey real (ex: VB-CABLE, captura de tecla) agora podem ser validadas de ponta a ponta, não só por leitura de código.
- **Timbre por efeito (não global).** Decisão do Bruno (2ª rodada): o slider de "Timbre" é lembrado separadamente por efeito de voz (Robô, Monstro, Alien, Esquilo, Demônio) — ao trocar de efeito, o slider restaura o valor daquele efeito especificamente, não um valor único compartilhado entre todos. Implica estado (`AppState::effectTimbre`) e persistência (`AppConfig::effectTimbre`) indexados por efeito, não um float solto.
- **Hotkey de bypass configurável pela mesma UI dos slots do soundboard.** Decisão do Bruno: como a UI de captura de tecla já existe para os slots, o atalho de bypass reusa exatamente o mesmo componente (`HotkeyCaptureButton`), incluindo suporte a modificador e a regra de conflito last-write-wins contra os slots — evita ter dois comportamentos divergentes pro mesmo tipo de controle.
- **Combinação de hotkey = um único modificador (Ctrl/Alt/Shift) + uma tecla F1–F12, não múltiplos modificadores simultâneos nem duas teclas em sequência.** Decisão do Analista, validada em uso pelo Bruno (ex: Ctrl+F1). É o padrão nativo do `RegisterHotKey` do Windows. Tecla Windows (`MOD_WIN`) ficou de fora deliberadamente — JUCE nem expõe a tecla Windows como modificador capturável em `ModifierKeys`, então a exclusão é estrutural, não só convenção.
- **VB-CABLE deixou de depender do dispositivo de saída padrão do Windows.** Causa raiz de um bug real relatado pelo Bruno: o app usava `deviceManager.initialiseWithDefaultDevices(...)`, então se o padrão de saída do Windows não fosse "CABLE Input" no momento (ex: mudou por qualquer motivo, inclusive sem o Bruno mexer diretamente), a voz processada saía direto nos fones dele em vez de ir só pro mic virtual do jogo — sintoma: "eu me escuto". Corrigido fazendo `VBCableDetector::detect()` enumerar os dispositivos de saída de verdade (Windows-only, atrás de guard de plataforma), encontrar "CABLE Input (VB-Audio Virtual Cable)" por prefixo case-insensitive, e o app forçar essa saída explicitamente na inicialização via `setAudioDeviceSetup`. Fallback real para o padrão do Windows se a detecção não achar nada ou a seleção falhar ao abrir (edge case tratado: `setAudioDeviceSetup` não reabre o device anterior sozinho em caso de erro — o JUCE deixa o app sem device algum — então o fallback re-chama `initialiseWithDefaultDevices` manualmente para restaurar de verdade).

## 5. Integrações externas / dependências entre projetos

- **VB-CABLE (VB-Audio)** — driver de áudio virtual de terceiro. Nesta rodada o Bruno instalou manualmente numa máquina Windows real (o instalador silencioso embutido previsto no ADR-001 continua não implementado — só a detecção/seleção automática do device passou a ser real, não a instalação do driver em si). Gratuito para uso pessoal (donationware); redistribuição para terceiros exigiria consulta à VB-Audio. `VBCableDetector::detect()` agora faz enumeração real de dispositivos de saída (Windows-only, atrás de guard de plataforma) e o app seleciona "CABLE Input" automaticamente quando encontrado, com fallback para o padrão do Windows quando não. Gap residual conhecido: `detect()` não registra sob qual `AudioIODeviceType`/driver o "CABLE Input" foi encontrado — se aparecer só sob um tipo diferente do atualmente ativo no `AudioDeviceManager`, a seleção pode falhar silenciosamente (cai no fallback, não trava, mas não rotea pra CABLE); não reproduzido na prática até agora.
- **SoundTouch** (pitch-shift) — decidido, ver ADR-004. Fonte: `codeberg.org/soundtouch/soundtouch` 2.4.1.

## 6. Áreas sensíveis / gotchas conhecidos

- **Thread-safety em tempo real:** a thread de áudio não pode alocar memória nem usar locks — qualquer bug aqui vira glitch audível ou crash durante uma partida.
- **Latência:** meta é <50-100ms ponta a ponta; precisa de tuning empírico de buffer WASAPI na máquina real do Bruno. ASIO fica em aberto só se o modo compartilhado não bastar.
- **Instalação/desinstalação do VB-CABLE:** lidar com driver já instalado previamente na máquina, versionamento e desinstalação limpa junto com o Equalizador.
- **Dependência de política de assinatura de driver da Microsoft** mudar no futuro — fora do controle do projeto; mitigado parcialmente pelo desacoplamento do endpoint de saída (ADR-001).
- **Hotkeys globais em fullscreen exclusivo:** recomenda-se orientar o uso de "Borderless Windowed" no CoD para máxima compatibilidade. `RegisterHotKey`/`Win32HotkeyBackend` já foi exercitado com sucesso em Windows real fora do jogo (captura de F1–F12 com e sem modificador, `Win32HotkeyBackend` reescrito nesta rodada pra empacotar binding em WPARAM/LPARAM e suportar `unregisterHotkey`/`unregisterAll` de forma thread-safe) — mas ainda não validado com o CoD em foreground/fullscreen exclusivo especificamente.
- **Escolha do algoritmo de pitch-shift** (qualidade vs. custo de CPU) precisa de prototipagem, especialmente rodando ao lado do CoD. Ficou mais relevante nesta rodada: agora são **4 efeitos baseados em `PitchShifter`** (Monstro, Alien, Esquilo, Demônio), cada um com sua própria instância preparada eagerly em `EffectChain::prepare()` (antes só o Monstro existia) — o custo de warm-up do FIFO do SoundTouch multiplicou por 4, ainda não medido empiricamente.
- **`PitchShifter`/SoundTouch não é comprovadamente RT-safe** — o FIFO interno da lib pode alocar; há warm-up de blocos de silêncio no `prepare()` para estabilizar, mas é garantia empírica, não formal. Agora sustenta 4 efeitos simultaneamente possíveis (não só 1) mais um parâmetro de timbre ajustável ao vivo por cima (delta de semitons aplicado via `setTimbre()`, mutação acontece dentro da thread de áudio, não da UI). Precisa validação com profiler (ex: xrun counter) na máquina Windows real antes de considerar o DSP fechado.
- **Race em `SoundboardEngine` — corrigida nesta rodada, após reprovação do Revisor.** A primeira tentativa (double-buffer de 2 slots fixos) ainda permitia use-after-free se `loadSlot()` fosse chamado duas vezes no mesmo slot antes da thread de áudio terminar de ler o buffer publicado pela primeira chamada. Solução final: sample publicado via `std::shared_ptr<const juce::AudioBuffer<float>>` trocado com `std::atomic_store_explicit`/`atomic_load_explicit` (C++17 — o projeto usa `CMAKE_CXX_STANDARD 17`, então não pôde usar `std::atomic<shared_ptr<T>>` direto, só padronizado no C++20); a thread de áudio tira sua própria cópia local do ponteiro no início do processamento de cada slot por bloco, e o refcount garante que o buffer não é liberado enquanto essa cópia estiver viva. **Esse caminho agora É exercitado de verdade em produção** (escolher arquivo pelo `FileChooser` e o carregamento de config na inicialização chamam `loadSlot()`), diferente da rodada anterior onde nada chamava esse método ainda.
- **`OutputRouter`/`AudioCaptureSource` truncam silenciosamente** se o driver mudar o buffer size em runtime sem novo ciclo de `prepare` — risco teórico no CoreAudio do Mac, mas relevante ao migrar para WASAPI real (shared/exclusive mode podem trocar buffer size). Ainda não tocado.
- **Auto-monitoração ("ouvir a mim mesmo") ainda não implementada** (`SelfMonitorOutput`) — exige duas streams WASAPI simultâneas com clocks independentes (drift em sessões longas), tratado como módulo próprio, não uma extensão trivial do `OutputRouter`. **Importante não confundir com** o bug (já corrigido, ver seção 4) de o app vazar a própria voz processada pros fones do usuário por causa do device de saída errado — eram sintomas parecidos ("eu me escuto"), causas totalmente diferentes: um é feature ausente, o outro era bug de seleção de device.
- **Persistência (`ConfigManager`/`ConfigSchema`) — conectada nesta rodada.** Existia e era testada isoladamente desde a 1ª rodada, mas `MainComponent` nunca chamava `load`/`save`; nenhuma tecla (nem bypass, nem soundboard) era de fato registrada (`keyCode = 0` sempre). Agora o construtor carrega a config persistida (arquivo de cada slot + hotkey de cada slot + hotkey de bypass + timbre por efeito) e toda mudança bem-sucedida salva imediatamente.
- **Lógica de conflito de hotkey (`MainComponent::registerExclusiveHotkey`) sem teste automatizado.** Decide, por par `(keyCode, modifiers)`, quem "rouba" um atalho já em uso (last-write-wins) entre os 12 slots e o bypass — é regra de negócio real, mas vive dentro de `MainComponent` (JUCE Component), camada que o projeto não cobre com `juce::UnitTest` por convenção. Revisor sugeriu extrair pra uma função pura testável numa rodada futura, dado o tamanho que essa regra já tem (5 ramos de decisão).

## 7. Log de atualizações

- **2026-07-12** — Criação inicial do contexto via pipeline `/orquestrador` (etapas Analista → PO → Arquiteto). Origem: `pipeline`. Consolidado a partir da primeira sessão de planejamento do projeto: requisitos levantados, MVP definido, arquitetura macro e ADRs de dispositivo de áudio virtual / pilha técnica / mixagem definidos. Nenhum código implementado ainda.
- **2026-07-13** — Primeira rodada de implementação via pipeline `/orquestrador` (etapas Designer → TL → Dev → Revisor). Origem: `pipeline`. Designer definiu UI (tema escuro, layout, indicador de bypass, wizard); TL fechou SoundTouch como lib de pitch-shift e entregou plano de 15 tarefas (~22-29 dias) com estrutura de módulos C++/JUCE; Dev implementou scaffold real (CMake+JUCE+SoundTouch), cadeia de áudio completa e RT-safe, hotkeys lock-free, mixer, soundboard, persistência JSON e UI mínima, compilando e rodando no Mac via CoreAudio (ambiente de dev; alvo final Windows/WASAPI) — 184 testes unitários passando; Revisor leu o código, rodou os binários e testes pessoalmente, e aprovou (✅) com 3 itens de dívida técnica não-bloqueantes registrados acima. Fora de escopo desta rodada, deliberadamente: `SelfMonitorOutput`, `VBCableDetector`/wizard real, `Win32HotkeyBackend` testado em Windows real, UI visual completa do Designer.
- **2026-07-13/14** — Segunda rodada, várias sessões via pipeline `/orquestrador` (perfil 🏃 Mínimo: Analista → Dev → Revisor, sem PO/Arquiteto/Designer/TL). Origem: `pipeline`. Primeiro build real em Windows (toolchain CMake+MSVC Build Tools instalada do zero) e primeira execução do app fora do Mac. Trabalho entregue, todas as rodadas aprovadas pelo Revisor:
  1. **Timbre por efeito + 3 vozes novas** (Alien/ET, Esquilo, Demônio) + **soundboard conectado de ponta a ponta** (escolha de arquivo, hotkey, persistência automática — antes existia só a tubulação isolada, nada chamava). Reprovado na primeira passada por race condition crítica em `SoundboardEngine::loadSlot()` (use-after-free real, não teórico); corrigido com `shared_ptr` atômico após retrabalho; aprovado na segunda revisão.
  2. **Bugfix: app vazava a própria voz processada pros fones do usuário** ("eu me escuto") porque dependia do dispositivo de saída padrão do Windows em vez de mirar explicitamente no VB-CABLE. `VBCableDetector` deixou de ser stub — detecção real + seleção automática de "CABLE Input" na inicialização, com fallback genuíno pro padrão do Windows (2 rodadas: 1ª aprovada com ressalva sobre o fallback não restaurar de verdade em caso de erro; ressalva corrigida na rodada seguinte).
  3. **Soundboard expandido de 8 para 12 slots** (cobertura completa de F1–F12) e **hotkeys passaram a aceitar um modificador** (Ctrl/Alt/Shift + tecla, ex: Ctrl+F1) tanto nos slots quanto no bypass — antes só aceitavam uma tecla F1–F12 pura.
  Dívida técnica nova registrada na seção 6: lógica de conflito de hotkey sem teste automatizado; `VBCableDetector` não rastreia sob qual driver/tipo o CABLE Input foi encontrado. 1281 testes unitários passando ao final. Fora de escopo, deliberadamente: instalador silencioso do VB-CABLE (Bruno instalou manualmente), `SelfMonitorOutput`, múltiplos modificadores simultâneos em hotkey, seleção manual de dispositivo de entrada/saída pela UI (`inputDeviceName`/`outputDeviceName` em `ConfigSchema` continuam órfãos).
